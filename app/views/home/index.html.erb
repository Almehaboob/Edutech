<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edutech</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>

  <!-- Font Awesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
  <style>
    /* Body and background image */
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      height: 100vh;
      background: url('https://cdn.pixabay.com/photo/2023/12/14/02/06/ai-generated-8448073_1280.png') no-repeat center center fixed;
      background-size: cover;
      display: flex;
      justify-content: center;
      align-items: center;
      color: white;
    }

    /* Overlay to make text readable */
    .overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0.5);
      z-index: 0;
    }

    /* Container for text and button */
    .landing-container {
      text-align: center;
      z-index: 1;
      padding: 15px;
      max-width: 400px;
      background: rgba(0, 0, 0, 0.7); /* Dark overlay for content */
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(138, 43, 226, 0.8); /* Violet shadow */
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 15px;
      font-weight: bold;
    }

    p {
      font-size: 1rem;
      margin-bottom: 20px;
    }

    .btn-login {
      background-color:#D8BFD8;
      color: black;
      padding: 10px 20px;
      font-size: 1rem;
      border: none;
      border-radius: 5px;
      text-decoration: none;
      display: inline-block;
      transition: background-color 0.3s ease;
    }

    .btn-login:hover {
      background-color: #0056b3;
      text-decoration: none;
    }

    .fa {
      margin-right: 8px;
    }
  </style>
</head>
<body>

    <div class="landing-container">
     <h1>Welcome to Edutech</h1>
     <p>Your gateway to interactive learning and growth.</p>
    <%= link_to new_user_session_path, class: 'btn-login' do %>
    <i class="fas fa-sign-in-alt"></i> Log In
    <% end %>
    </div>

</body>
</html>